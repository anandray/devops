apiVersion: v1
kind: Pod
metadata:
  creationTimestamp: null
  labels:
    run: nginx
  name: nginx
spec:
  volumes:
  - name: mysecret2
    secret:
      secretName: mysecret2
  containers:
  - image: nginx
    name: nginx
    volumeMounts:
    - name: mysecret2
      mountPath: "/etc/foo"
    resources: {}
  dnsPolicy: ClusterFirst
  restartPolicy: Never
status: {}
